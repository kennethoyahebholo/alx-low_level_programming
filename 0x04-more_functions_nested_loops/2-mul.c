#include "main.h"

/**
 * mul - returns result of multiplication between 2 integers.
 * @a: first multiplier.
 * @b: second multiplier.
 * Return: multiplication result
 */

int mul(int a, int b)
{
	return (a * b);
}
